---
import SiteLayout from "../layouts/SiteLayout.astro";
---

<SiteLayout title="Not Found" nav={[]}>
  <h1 class="text-2xl font-semibold">Not found</h1>
  <p class="mt-2 text-slate-600">That page doesn’t exist.</p>
</SiteLayout>